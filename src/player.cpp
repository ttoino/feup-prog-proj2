#include "player.h"

using namespace std;

Player::Player(line, collune)
{
	this->line = line;
	this->collune = collune;
	alive = true;
}
Player::setAlive(&alive)
{
	this->alive=alive
}
Player::movePlayer(line, collune)
{
	this->line = line;
	this->collune = collune;
}
