#include "robot.h"

using namespace std;

Robot::Robot(line, collune)
{
	this->line = line;
	this->collune = collune;
	alive = true;
}
Robot::setAlive(&alive)
{
	this->alive = alive
}
Robot::moveRobot(line, collune)
{
	this->line = line;
	this->collune = collune;
}